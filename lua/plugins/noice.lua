return {
  {
    -- "folke/noice.nvim",
    -- lazy = true,
    -- event = "VeryLazy",
    -- dependencies = {
    --   "MunifTanjim/nui.nvim",   -- required for UI components
    --   "rcarriga/nvim-notify",   -- optional, for fancy notifications
    -- },
    -- config = function()
    --   require("noice").setup({
    --   cmdline = {
    --     format = {
    --       cmdline = { pattern = "^:", icon = "", lang = "vim" },
    --       search_down = { kind = "search", pattern = "^/", icon = " " },
    --       search_up = { kind = "search", pattern = "^%?", icon = " " },
    --     },
    --     view = "cmdline_popup",   -- show dynamic command output
    --     opts = {
    --       position = { row = 5, col = "50%" },
    --       size = { width = 60, height = "auto" },
    --     },
    --   },
    --
    --   messages = {
    --     view = "notify",  -- or "mini" for small messages
    --     view_error = "notify",
    --     view_warn = "notify",
    --     view_history = "split",  -- optional, history in split
    --   },
    -- })
    -- end,
  }
}
